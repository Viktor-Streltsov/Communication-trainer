fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.3.0
httpx>=0.27.0
python-dotenv>=1.0.1

# Database
sqlalchemy>=2.0.0
# psycopg3: modern driver, works with Python 3.12+ on Windows, supports both
# sync (for Alembic) and async (for FastAPI) with the same dialect name.
psycopg[binary]>=3.1.0
alembic>=1.13.0
